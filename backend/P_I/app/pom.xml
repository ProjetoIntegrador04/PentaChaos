<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>projeto_integrador</groupId>
    <artifactId>P_I</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>


  <artifactId>app</artifactId>
  <name>Gestao Estagiarios - App</name>
  <description>Aplicação Spring Boot (módulo executável)</description>
  <packaging>jar</packaging>

  <properties>
    <!-- REMOVIDO: java.version aqui. Ele será herdado do POM pai (Java 21). -->
  </properties>

  <dependencies>
    <!-- Web/API -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Validação (Jakarta Bean Validation) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- JPA -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <!-- JDBC (opcional, útil para scripts e healthchecks) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>

    <!-- Flyway -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
  
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- ADICIONADO: Dependência para o módulo 'shared' -->
    <dependency>
      <groupId>projeto_integrador</groupId>
      <artifactId>shared</artifactId>
    </dependency>

    <!-- DEPENDÊNCIAS DOS MÓDULOS INTERNOS (groupId CORRIGIDO, version REMOVIDO) -->
    <dependency>
      <groupId>projeto_integrador</groupId> <!-- CORRIGIDO -->
      <artifactId>iam</artifactId>
      <!-- REMOVIDO: <version>${project.version}</version> (gerenciado pelo POM pai) -->
    </dependency>
    <dependency>
      <groupId>projeto_integrador</groupId> <!-- CORRIGIDO -->
      <artifactId>squads</artifactId>
      <!-- REMOVIDO: <version>${project.version}</version> -->
    </dependency>
    <dependency>
      <groupId>projeto_integrador</groupId> <!-- CORRIGIDO -->
      <artifactId>tasks</artifactId>
      <!-- REMOVIDO: <version>${project.version}</version> -->
    </dependency>
    <dependency>
      <groupId>projeto_integrador</groupId> <!-- CORRIGIDO -->
      <artifactId>frequency</artifactId>
      <!-- REMOVIDO: <version>${project.version}</version> -->
    </dependency>
    <dependency>
      <groupId>projeto_integrador</groupId> <!-- CORRIGIDO -->
      <artifactId>reports</artifactId>
      <!-- REMOVIDO: <version>${project.version}</version> -->
    </dependency>
    <dependency>
      <groupId>projeto_integrador</groupId> <!-- CORRIGIDO -->
      <artifactId>platform</artifactId>
      <!-- REMOVIDO: <version>${project.version}</version> -->
    </dependency>

    <!-- Testes -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Opcional: Testcontainers para integrações com Postgres -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>